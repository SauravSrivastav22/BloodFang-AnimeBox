# Loaded by `vite build --mode static` (the Firebase-hosted build).
# Routes anime data straight to AniList from the browser instead of the /api
# Express backend, so the app can be served as pure static files.
VITE_DATA_MODE=direct
