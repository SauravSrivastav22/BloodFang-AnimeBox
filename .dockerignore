# Keep the backend image small — only package manifests + server/ are copied
# in the Dockerfile anyway, but this avoids sending the rest as build context.
node_modules
dist
.git
.firebase
.claude
src
public
*.md
*.zip
.env*
firebase.json
firestore.rules
vite.config.js
index.html
